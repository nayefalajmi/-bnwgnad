-- ════════════════════════════════════════════════════════════════
--  عمود القطعة (block) على جدول الطلبات
--
--  يخزّن رقم/اسم القطعة منفصلاً (إضافةً إلى وجوده داخل نص address).
--  يفيد في الفرز والتصفية والتصدير حسب القطعة لاحقاً.
--  يُعبّأ تلقائياً من index.html (نقدي) و api/create-payment.js (بطاقة).
--
--  شغّله مرة واحدة في: Supabase Dashboard ▸ SQL Editor ▸ Run.
-- ════════════════════════════════════════════════════════════════

alter table orders add column if not exists block text;
